{
  "version": "1.0.0",
  "description": "A test package without a name field - should cause errors",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}

